import { BrowserRouter as Router } from 'react-router-dom';
import ReactRouter from './components/ReactRouter';

function App() {
    return (
        <Router>
            <ReactRouter />
        </Router>
    );
}

export default App;
